# Include these prefixes before the general /media/ location.
# Django now serves post covers and body images through permission-checked URLs.
location ^~ /media/covers/ {
    return 404;
}

location ^~ /media/post_images/ {
    return 404;
}
