# Trust the original HTTPS scheme only when the local tunnel supplied it.
map "$remote_addr:$http_x_forwarded_proto" $clover_forwarded_proto {
    default $scheme;
    "127.0.0.1:https" https;
    "::1:https" https;
}
